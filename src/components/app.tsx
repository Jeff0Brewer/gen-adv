import type { ReactElement } from 'react'
import styles from '../styles/app.module.css'

function App (): ReactElement {
    return (
        <main className={styles.textView}>
            <p>hi</p>
        </main>
    )
}

export default App
